---
name: react-best-practices
description: Best practices for React/TypeScript development with hooks, state management, and component patterns
license: MIT
---

## What I do
This skill guides React/TypeScript development following modern best practices.

## Component Patterns
- Use functional components with hooks
- Prefer composition over prop drilling
- Keep components small and focused (single responsibility)
- Extract reusable logic into custom hooks

## TypeScript Guidelines
- Define interfaces for all component props
- Use explicit types over `any`
- Export types that are used across files

## State Management
- Use `useState` for local component state
- Lift state up only when necessary
- Use `useCallback` and `useMemo` for performance optimization

## When to use me
- Building new React components
- Refactoring existing components
- Adding TypeScript types
- Setting up state management patterns
